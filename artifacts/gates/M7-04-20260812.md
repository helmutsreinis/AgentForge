# Gate M7-04-20260812

Decision: **Pass**

## Scope

Milestone 7 exit: loopback web setup over shared services with one-time nonce, authenticated session, CSRF,
idempotency, safe credential staging, CLI-equivalent profile defaults, refreshed UI, and complete dual-platform
regression over search, memory, channels, and setup.

## Verification evidence

| Command/check | Windows | Ubuntu 24.04 |
|---|---:|---:|
| Locked restore and Release build | Pass | Pass |
| Unit | 252/252 | 252/252 |
| Integration | 54/54; 1 live skipped | 54/54; 1 live skipped |
| Architecture | 4/4 | 4/4 |
| Security | 11/11 | 11/11 |
| Cross-platform | 14/14 | 14/14 |
| End-to-end | 10/10 | 10/10 |
| Product total | 345/345 | 345/345 |
| Framework spike | 2/2 | 2/2 |
| Format | Pass | Pass |
| Migration drift and vulnerability scan | Pass / clean | shared schema / locked restore clean |
| In-app browser visual/DOM smoke | Pass; no console errors | N/A |

The web journey proves nonce consumption, HttpOnly cookie handling, missing-CSRF denial, serialized concurrent
exact-idempotency replay, provider credential configuration, policy preview, agent creation, Ready completion, post-completion
lockout, and persisted equality with CLI safe defaults.

The visual smoke confirmed the complete first-run form, status cards, accessible labels, enabled configure
action, local-control-plane title, and absence of browser console errors at the default desktop viewport.

## Security and portability

Web setup is independently loopback/origin guarded even if the host is rebound. Sessions expire after 20
minutes and become exact-replay-only on completion. Credentials use bounded strict-UTF8 clearable server
buffers; the browser clears its field immediately. Responses are no-store and existing CSP/anti-frame headers
remain. The deterministic secret store in E2E is a test fixture only; production retains DPAPI/Secret Service.

## Remaining equipped gates

Docker remains unavailable locally. Container sandbox/image tests stay on equipped CI runners. Live search,
channel, and hardware gates remain credential/device gated and do not affect deterministic acceptance.

## Rollback

Stop the host to invalidate in-memory setup sessions, remove the web endpoints/assets/session manager, and
retain the complete CLI setup path. This slice adds no schema migration.
