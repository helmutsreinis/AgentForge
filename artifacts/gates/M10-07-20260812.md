# Gate M10-07 — Durable outbox and complete passive environment profile

Status: **Pass**

## Scope

- Close AF-DATA-001 with the harness-owned transactional outbox contract.
- Close AF-ENV-001 with bounded shell, package-database, and aggregate network evidence.

## Evidence

- `Event_outbox_commits_atomically_orders_pending_and_rejects_stale_completion` passes against SQLite and covers uncommitted invisibility, deterministic UTC order, atomic commit, optimistic completion, and stale replay denial.
- `EnvironmentProfileBuilderTests` passes schema-v2 normalization, inventory-order-independent hashing, Linux distribution/Kali rules, and Windows path semantics.
- `SystemEnvironmentProfilerTests` passes passive live capture on Windows with strict collection and network bounds. The same suite is part of the Linux R1 matrix.
- `dotnet build tests/AgentForge.UnitTests/AgentForge.UnitTests.csproj -c Release --no-restore` passes with zero warnings.

## Security and portability

- The profiler uses native metadata and bounded file/registry reads; it never starts an inventoried candidate or opens a network connection.
- Network evidence is aggregate interface state only. Package evidence is database identity and a bounded count, never package command output.
- SQLite cannot portably order `DateTimeOffset`, so the outbox persists an explicit UTC tick key that is translated by both EF providers.

## Rollback

Preserve the upgraded directory, verify the pre-upgrade backup, and restore it into a
separate empty target using the prior binary. Do not apply a destructive down migration
to operator data.
