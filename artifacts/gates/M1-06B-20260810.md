# Gate M1-06B-20260810

Decision: **Pass**

## Scope

Setup maintenance and recovery: typed doctor checks, authenticated redacted export,
content-addressed report/rollback artifacts, immutable snapshot metadata, automatic
pre-recovery capture, version-bound recovery transitions, authenticated recompletion,
credential-shaped metadata rejection, migration 0005, and CLI cancellation.

## Requirements addressed

AF-SET-004 is `Verified`. The transition portion of AF-SET-005 and the metadata/export
portion of AF-SEC-003 pass but remain `In progress` until configuration edits, restore,
and model-context handling complete. AF-SET-002 remains open for the complete provider
CLI and full interactive/headless profile-equivalence gate.

## Verification evidence

| Command/check | Result |
|---|---|
| Windows/Ubuntu locked restore and Release build | Pass; .NET 10.0.302, 0 warnings, 0 errors |
| Windows/Ubuntu full suite | Pass; 55/55 tests on each platform |
| Windows/Ubuntu format verification | Pass |
| Doctor | Pass: bounded storage lock/read/write, audit, secret-store, state, provider, agent, administrator checks |
| Export redaction | Report/profile hashes persist; verifier and materialized administrator credential are absent |
| Recovery authority | Wrong credential denied; stale version conflicts; exact actor credential enters/resumes/recompletes |
| Automatic rollback | `Ready` profile snapshot commits with recovery state and audit; snapshot count and hash verified |
| Windows CLI maintenance | Live DPAPI doctor/export/enter/doctor/resume/recomplete journey passes |
| Migration 0004 to 0005 | Existing installation and administrator survive; no snapshot is fabricated |
| Pending-model check | No model changes after migration 0005 |
| Host smoke | Both platforms return `live=200 setup=200 runtime=503` for a clean setup-only host |
| Dependency and secret scan | No vulnerable packages; scan passes across 193 tracked files |
| Diff hygiene | `git diff --check` passes |

Migration SHA-256:
`5ca44b8c81facff084941d457576ddebb42c616d6fdc6d27f39922f55ee0326a`.

## Security and portability review

Maintenance mutations require exact versions and the matching administrator. The CLI
never accepts the credential in arguments; it materializes the OS reference into a
disposable lease and propagates cancellation. Credential-shaped actor, correlation,
provider, and agent content is rejected before persistence. Export payloads are
redacted before artifact storage and contain references only.

SQLite/artifact behavior and deterministic credentials pass identically on Windows
and Ubuntu. Windows additionally passes the current-user DPAPI CLI journey. Ubuntu
continues to report a missing Secret Service as typed unsupported and never degrades.

## Remaining findings

- Provider/agent edit preview/apply, snapshot restore, complete provider CLI setup,
  and full profile-equivalence evidence remain M1 slice 6c.
- Denied-request audit policy, idempotency, rate limiting, session/lockout controls,
  browser CSRF, and remote TLS remain later control-plane gates.
- Docker remains unavailable for the M2 container-isolation gate.

## Rollback

Stop AgentForge and restore the pre-0005 SQLite, artifacts, and OS-reference set as one
cold backup. Do not apply migration 0005 down to operator state because it deletes
recovery evidence. A failed relational snapshot/transition commit can leave only an
unreferenced immutable redacted artifact; it cannot authorize recovery or runtime.
