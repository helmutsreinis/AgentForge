# Gate M10-05 — Policy-filtered MCP client transports

Status: **Pass**

## Scope

- Client portion of AF-MCP-001 over official stdio and Streamable HTTP SDK transports.
- Exact process, destination, credential, catalog, invocation, and result boundaries.

## Evidence

- `dotnet build tests/AgentForge.IntegrationTests/AgentForge.IntegrationTests.csproj -c Release --no-restore` — pass, zero warnings.
- `dotnet test tests/AgentForge.UnitTests/AgentForge.UnitTests.csproj -c Release --no-restore --filter FullyQualifiedName~McpRemoteProfileValidatorTests` — 2/2 pass.
- `dotnet test tests/AgentForge.IntegrationTests/AgentForge.IntegrationTests.csproj -c Release --no-build --filter FullyQualifiedName~ProductionApiTests` — 2/2 pass.
- `dotnet format AgentForge.slnx --no-restore --verify-no-changes` — pass.
- The harness-owned client connected through the official Streamable HTTP transport to the authenticated production TestServer, filtered discovery, invoked/read only exact allowlisted names, and denied an unlisted tool before transport.

## Security and portability

- Stdio admits only an existing absolute command and working directory, inherits no environment, accepts no credential, and receives arguments as an array.
- HTTP disables redirects, cookies, decompression, and ambient proxy behavior; each socket rejects a mixed or out-of-scope DNS result.
- Public HTTP is HTTPS-only and requires an OS-backed secret reference materialized for one request. Loopback permits credential-free local integrations.
- Client result and argument documents, discovery counts, fields, and configured allowlists are bounded identically on Windows and Linux.

## Rollback

Revert this slice. The MCP server and production API remain independently available and no durable schema changed.
