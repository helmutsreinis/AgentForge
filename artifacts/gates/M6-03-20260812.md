# Gate M6-03-20260812

Decision: **Pass**

## Scope

Milestone 6 exit: durable native coding sessions, typed plans, content-addressed diffs, per-command
checkpoints, failure/resume, exact Git review, sample implementation, and complete Windows/Linux
coding journey over the prior worktree, semantic, patch, backend, and verifier slices.

## Verification evidence

| Command/check | Result |
|---|---|
| Durable sample journey | Pass; faulty Calculator baseline, isolated patch, real `dotnet build`, real executable spec, interruption, resume, exact review, completion v7, replay |
| Command checkpoint recovery | Pass; build called once, interrupted test called twice, completed build never repeated |
| Isolation/privacy | Pass; baseline source unchanged, exact one-file Git diff, objective/patched source absent from SQLite, output stored as hashes |
| Pure coding state tests | Pass; ordered verification, out-of-order denial, canonical chain/tamper, full completion, cancellation terminality |
| Windows/Ubuntu locked Release suites | Pass on each platform; 317/317 product tests plus 2/2 framework-spike tests; one credential-gated live test skipped |
| Migration/format/architecture/vulnerability | Pass; migration 0018 matches model; format and 4/4 architecture tests pass; no vulnerable package found |

Evidence SHA-256:

- coding session state machine: `e67056e19a29531cd5b2d924ad50808a8147047b17058b243c7e70b9ba99b478`
- durable workflow service: `c29e2cf5151d73d102e30510b9f08d89d1fa41697bf7e1dd7d7566ab4ccb3196`
- exact Git reviewer: `4b26895abc0812a689481e07b0fdea9c187eac81256965b895f5200d61396cef`
- migration 0018: `ad51d6e827a10c51bff1fc96c992c7083fd3986219159c59893700a8f8061820`
- durable sample test: `ae68c9f70634752e7d8135f83e5c156d6226cd5c3205536d465729f000c22a20`

## Security and portability review

Session creation binds exact immutable authority and backend evidence. Backends see no local root and
only propose a validated patch. Objectives are hash-only, patch bodies are artifact-only, verifier
stdout/stderr are hash-only, and audit contains state/evidence IDs rather than content. Each command
checkpoint commits before the next command. Review derives changed paths and diff hash directly from
Git and denies unrelated changes.

The same Git, MSBuild/Roslyn, patch, state, persistence, actual sample build/test, interruption, and
resume journey passes on Windows and Ubuntu. Production project execution still requires a real
denied-network container adapter; the test-only process-backed contract fixture is explicitly not
production isolation and does not alter that fail-closed requirement.

## Remaining findings

- Docker is not installed locally. A real container-sandbox invocation remains the equipped-runner
  gate; production returns typed UnsupportedCapability instead of using the test fixture.
- No public coding mutation endpoint is exposed. Host composition has an empty backend catalog.

## Rollback

Restore the complete pre-0018 backup. Remove coding session/review service and contracts, snapshot
entity/repository/migration, sample and acceptance tests, host composition, ADR, gate, and traceability.
