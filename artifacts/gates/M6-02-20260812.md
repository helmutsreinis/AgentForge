# Gate M6-02-20260812

Decision: **Pass**

## Scope

Milestone 6 slice 2: hash-bound unified patch application, patch-only backend and read-only language
server contracts, exact backend catalog, and harness-owned isolated verification plans.

## Verification evidence

| Command/check | Result |
|---|---|
| Patch and verifier matrix | Pass; 4 focused tests plus prior workspace/semantic tests |
| Patch safety | Pass; exact before hash, conflicting replay, canonical hunk validation, whole-set preflight, no partial mutation |
| Verification safety | Pass; exact authority/plan binding, denied-network container/filesystem requirement, bounded output hashes, unsafe fallback denial, publish approval |
| Windows/Ubuntu focused gate | Pass on both platforms |
| Format and architecture | Pass |

Evidence SHA-256:

- patch applier: `df2d6dda6c260b4045800120d27aba3ec771d4c9ca343d35f6e37a4b6090c96b`
- sandbox verifier: `a8628532882c29369a826ff0f62072bca97a627183ad03fe21e8f566929797bf`
- backend catalog: `d9f31286d405e73cfa381ec2afec86d4f05aa869a89cc7516967656498fee820`
- focused tests: `c591eec805f00ee759191371cfada485ab8f70e5483643dfd59cdb5959925883`

## Security and portability review

Patch input cannot select rooted/traversing/linked paths, substitute headers, exceed file/set bounds,
or apply stale context. Writes stage under the worktree and retain recoverable backups until every
move succeeds. Backend contracts expose hashes/relative metadata but no local root and return only
patch proposals.

All project-executing verifier kinds require container, denied network, filesystem and network
isolation. Review-only commands may use a restricted host but still receive bounded literal
arguments. Verification outputs are reduced to hashes. Publish remains independently approval-bound.

## Remaining findings

- Docker is not installed, so live container execution remains the named Milestone 2/6 equipped-runner
  gate; deterministic sandbox evidence covers orchestration without weakening production requirements.
- Durable coding sessions, checkpoint resume, audit, and the complete sample change are the next slice.

## Rollback

Remove patch/verifier/backend/LSP contracts and implementations, records, tests, decision/threat
updates, traceability, and DI registrations. No schema changes are part of this slice.
