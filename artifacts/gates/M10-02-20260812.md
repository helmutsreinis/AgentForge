# Gate M10-02 — Signed plugin SDK and constrained worker

Status: **Pass**

## Scope

- AF-PLUGIN-001 stable plugin SDK, catalog, trust, isolation plan, and worker protocol.
- Production composition without automatic discovery or loading.

## Evidence

- `dotnet restore AgentForge.slnx --locked-mode` — pass.
- `dotnet build AgentForge.slnx --no-restore` — pass, zero warnings.
- `dotnet test tests/AgentForge.UnitTests/AgentForge.UnitTests.csproj --no-build --filter FullyQualifiedName~PluginIsolationTests` — 5/5 pass.
- `dotnet test tests/AgentForge.ArchitectureTests/AgentForge.ArchitectureTests.csproj --no-build` — 5/5 pass.
- `dotnet format AgentForge.slnx --no-restore --verify-no-changes` — pass after formatting verification.

## Security and portability

- Discovery reads manifests and assemblies only; no candidate assembly is loaded.
- Signature presence is not trust. The signature verifier result and low risk are both required in-process.
- Post-discovery hash changes fail with a concurrency conflict.
- All other plugins require a container worker with denied network and filesystem/resource/process isolation; the
  default restricted host cannot satisfy the request and fails typed.
- The worker and SDK target self-contained .NET 10 Windows and Linux release packaging.

## Rollback

Revert this slice. No schema changed and the production host does not automatically discover or activate plugins.
