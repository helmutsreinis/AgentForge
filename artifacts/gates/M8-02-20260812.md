# Gate M8-02-20260812

Decision: **Pass**

## Scope

Exact serial session grants, bounded immutable capture artifacts, durable metadata, and deterministic replay.

## Evidence

- Release solution build: pass, zero warnings.
- Focused capture/session unit tests: 5/5.
- Durable SQLite restart fixture: 1/1.
- EF Core pending-model check: no changes.
- Capture fixture proves byte truncation, accurate drop-only/disconnect accounting, exact idempotent non-reexecution,
  metadata-only relational storage, artifact existence, audit-chain integrity, and byte-exact replay after restart.
- Tamper fixture corrupts the stored artifact and replay fails closed.
- Session fixture proves capture does not grant read/write and only exact read/write grants call the adapter.

## Security and portability

The production transport catalog is empty and no public I/O route exists. The deterministic binary codec specifies
little-endian fields and fixed UTF-8 identity, independent of host endianness/path conventions. Raw bytes remain only in
content-addressed artifacts; relational and audit evidence contain hashes/counts. Live hardware remains a named equipped
gate.

## Rollback

Stop AgentForge and restore the complete pre-0021 database plus artifact directory. Do not apply down migration to erase
capture provenance. Removing Devices registration returns the runtime to unsupported transport behavior.
