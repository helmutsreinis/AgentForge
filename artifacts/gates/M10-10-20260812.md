# Gate M10-10 — Production R1 acceptance and release

Status: **Pass**

## Scope

- AF-QUAL-001
- All master-prompt Milestones 0–10
- All 25 end-to-end acceptance scenarios
- Production R1 source commit `2d798c21612a05282078c2f348138617e16e2f8b`

## Deterministic and cross-platform evidence

- Windows Release: 393 product tests pass and 2 Agent Framework spike tests pass; zero failures; four explicitly credential/PostgreSQL/Docker-gated live cases skip.
- Ubuntu 24.04 Release: the same 393 product tests and 2 spike tests pass; zero failures; the same four named live gates skip.
- The local package builder produces and verifies self-contained `win-x64` and `linux-x64` archives, complete SHA-256 manifests, checksums, and SPDX evidence. Both packaged host/CLI pairs pass their native OS smoke tests.
- [Windows and Ubuntu CI](https://github.com/helmutsreinis/AgentForge/actions/runs/31559773267) passes locked restore, zero-warning build, complete tests, merged coverage thresholds, format, dependency audit, release-default validation, self-contained package smoke, and secret scan on both hosted operating systems.
- [Equipped release workflow](https://github.com/helmutsreinis/AgentForge/actions/runs/31559776310) passes both self-contained package builds/smokes, Linux and Windows keyless provenance, artifact uploads, the non-root secure-default container smoke, and the real digest-pinned denied-network sandbox adapter test. A transient Windows Rekor fetch timeout passed on the failed-job-only retry with unchanged source.

## Acceptance evidence

`artifacts/acceptance/R1-execution.json` is generated by `AgentForge.Release.AcceptanceEvidenceGenerator/v1` from the two authoritative TRX sets. It contains exactly AC-01 through AC-25, and each scenario records:

- `status: Pass`;
- exact requirement IDs and source commit;
- configuration, manifest, transcript, test-result, and gate hashes;
- allowed live-gate disposition and external package-smoke evidence;
- audit and trajectory references.

The generator rejects an unknown requirement, missing/non-passing gate, absent test, unapproved skip, mismatched external evidence, unsafe output, or non-exact scenario set. The first final run detected four stale class selectors and produced no output; the manifest was corrected to the actual partial-class test identities and its own negative end-to-end tests pass.

## Security and release decision

The dependency audit and repository secret scan pass. Critical coverage exceeds 90% for policy/approval, state machines, audit/trajectory, and promotion/rollback. `docs/SECURITY_REVIEW_R1.md` records no unresolved High or Critical finding under the local single-operator R1 boundary.

Decision: **Production R1 passes.**

## Rollback

Do not destructively migrate backward. Preserve the exact release archive/checksum/SBOM and restore the previous verified complete installation package into a separate empty target as documented in `docs/UPGRADE.md`. If container or live-integration evidence becomes unavailable, disable the affected capability; never fall back to a weaker adapter.
