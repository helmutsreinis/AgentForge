# Gate M10-01 — Production API and MCP server transports

Status: **Pass**

## Scope

- AF-HOST-001..003 production REST/SSE hardening.
- Server portion of AF-MCP-001 over stdio and stateless Streamable HTTP.
- Exact authentication, idempotency, correlation, allowlist, origin, rate, request-size, and remote-binding controls.

## Evidence

- `dotnet restore AgentForge.slnx --locked-mode` — pass after lock refresh.
- `dotnet build src/AgentForge.Cli/AgentForge.Cli.csproj --no-restore` — pass, zero warnings.
- `dotnet format AgentForge.slnx --no-restore --verify-no-changes` — pass.
- `dotnet test tests/AgentForge.IntegrationTests/AgentForge.IntegrationTests.csproj --no-restore --filter FullyQualifiedName~ProductionApiTests` — 2/2 pass.
- The authenticated test uses the official MCP client against TestServer, lists the exact tool/resource, invokes readiness, verifies task mutation replay/conflict, and consumes SSE.
- Insecure non-loopback HTTP configuration fails options validation before serving.

## Security and portability

- HTTP MCP requires the same local-administrator credential as the production API; secrets are never MCP results.
- The stdio host writes logs to stderr and refuses non-Ready installations.
- Tool/resource exposure is an exact bounded allowlist and missing policy denies.
- Both official transports target .NET 10 and are covered by the final Windows/Ubuntu gate.

## Rollback

Revert the slice commit. The existing loopback status/setup surfaces and durable schema are unchanged; this slice
adds no database migration.
