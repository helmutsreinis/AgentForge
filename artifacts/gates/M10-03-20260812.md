# Gate M10-03 — PostgreSQL parity and backup/restore

Status: **Pass**

## Scope

- AF-DATA-002 Npgsql provider parity behind existing repositories.
- First-release PostgreSQL schema marker and migration/rollback instructions.
- Hash-manifested SQLite and PostgreSQL database-plus-artifact backup/restore.

## Evidence

- `dotnet restore AgentForge.slnx --force-evaluate` followed by `--locked-mode` — pass.
- `dotnet build AgentForge.slnx --no-restore` — pass, zero warnings.
- `dotnet test tests/AgentForge.IntegrationTests/AgentForge.IntegrationTests.csproj --no-build --filter FullyQualifiedName~PostgreSqlPersistenceTests` — 3 pass, 2 named live gates skipped.
- PostgreSQL model generation contains all current tables plus `citext` and no SQLite `NOCASE`.
- Deterministic exact-tool fixture proves custom dump/clean restore, `PGPASSWORD`, password-free arguments,
  separate target authority, bounded child execution, and package verification.
- SQLite online backup restores installation and artifact data into a new directory; tampering fails verification.

## Named live gates

- `AGENTFORGE_TEST_POSTGRESQL_CONNECTION` activates bootstrap/repository restart.
- Adding `AGENTFORGE_TEST_POSTGRESQL_RESTORE_CONNECTION`, `AGENTFORGE_TEST_PG_DUMP`, and
  `AGENTFORGE_TEST_PG_RESTORE` activates native backup/restore into disposable databases.
- Docker is unavailable locally, so these remain credential/tool-gated and run on equipped release agents.

## Security and portability

- Connection secrets are process-environment-only and sensitive EF logging is disabled.
- PostgreSQL tools are fully qualified, shell-free, argument-array invocations with an otherwise empty environment,
  bounded output/time, and process-tree termination.
- Restore rejects source authority and requires a separate empty artifact target.

## Rollback

SQLite schema is unchanged. For PostgreSQL, stop the host and restore the pre-bootstrap/provider-native snapshot;
do not delete or edit the R1 marker. Reverting code does not mutate either store.
