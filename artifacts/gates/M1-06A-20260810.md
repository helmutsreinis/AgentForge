# Gate M1-06A-20260810

Decision: **Pass**

## Scope

Minimum-viability completion and local administration: random OS-backed credential,
verifier-only persistence, fixed-time authentication, completion validation,
credential compensation on relational failure, guarded Ready transition, protected
runtime ping, migration 0004, and headless CLI completion.

## Requirements addressed

AF-SET-003 is `Verified`. The authentication portion of AF-HOST-003 and secret
handling in AF-SEC-003 remain `In progress`; mutation idempotency, rate limits,
sessions, export redaction, and remote controls remain open. AF-SET-004/005 remain
open for report/snapshot/edit/recovery work in M1 slice 6b.

## Verification evidence

| Check | Result |
|---|---|
| Windows/Ubuntu locked restore and Release build | Pass; 0 warnings, 0 errors |
| Windows/Ubuntu full test suite | Pass; 51/51 tests |
| Windows/Ubuntu format verification | Pass |
| Minimum viability | Audit, materializable text-provider secret, named agent, storage, administrator checks pass |
| Credential protection | 256-bit random client value; OS reference plus PBKDF2-SHA256 verifier only; fixed-time compare |
| Correct/wrong authentication | Correct actor returned; wrong credential receives `PolicyDenied` |
| Plaintext scan | Exact materialized administrator credential absent from SQLite bytes |
| Windows CLI completion | Live current-user DPAPI completion reaches Ready and emits reference only |
| Ubuntu live secret store | Missing Secret Service remains typed unsupported; deterministic completion passes |
| Migration 0003 to 0004 | Existing installation/provider/agent survives; no administrator is fabricated |
| Pending-model check | No model changes after migration 0004 |
| Host smoke | `live=200 setup=200 runtime=503` before completion on both platforms |

Migration SHA-256:
`b129ed3bc45bd5460b1ff3c6c7fddec72fa769080f24a6cce25991c9c2f65d21`.

## Security and portability review

Credential bytes and characters are cleared after derivation/storage; disposable
leases clear materialized values. Only salt/verifier/reference persist. Completion
rechecks all evidence, uses the pure two-step state path, and commits final state,
administrator, and redacted audit atomically. A failed commit triggers exact-reference
deletion. Ready runtime access requires a bounded bearer value and never logs it.

PBKDF2 and fixed-time comparison are platform-neutral. DPAPI completion passes live
on Windows. Ubuntu passes deterministic completion and explicit Secret Service
unavailability; its live completion gate requires `secret-tool` and an active DBus
secret-service session.

## Remaining findings

- Doctor, redacted report/export, rollback snapshot, edit/diff, and authorized
  recovery remain M1 slice 6b.
- API rate limiting, request-bound idempotency, session/lockout behavior, and all
  authenticated mutations remain later security/API gates.
- Docker remains unavailable for M2 container isolation.

## Rollback

Stop AgentForge and restore the pre-0004 SQLite/artifact/OS-reference backup set.
Never apply migration 0004 down to live state. If the relational commit fails during
first completion, AgentForge deletes the newly created OS secret by exact reference
and remains `Configuring`.
