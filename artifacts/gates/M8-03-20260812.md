# Gate M8-03-20260812

Decision: **Pass**

## Scope

Declarative serial decoding, deterministic evaluation, durable proposal governance, canary, quarantine, promotion, and rollback.

## Evidence

- Windows and Ubuntu 24.04 locked restore and Release build: pass, zero warnings.
- Windows/Ubuntu product suites: 362/362 each (unit 265, integration 56 with one live skip, architecture 5,
  security 11, cross-platform 15, end-to-end 10); framework spike 2/2 each.
- Format verification: pass on Windows and Ubuntu. EF pending-model check: clean. Vulnerability scan: clean.
- Decoder/parser/evaluator/state-machine unit tests: 5/5.
- Durable decoder-governance restart fixture: 1/1.
- Parser fixture proves typed values, exact unknown frame bytes, unframed noise, and raw input accounting.
- Evaluation fixture passes target, holdout, malformed, partial, concatenated, resynchronization, 256 deterministic fuzz cases,
  unknown preservation, and operation limits; a failed holdout deterministically vetoes approval.
- Governance fixture proves proposer/evaluator/approver/governor separation, permission-escalation rejection, append-only hash
  chain, stale competing-candidate denial, atomic active selection, restart, audit integrity, and rollback.
- Failed canary evidence transitions to quarantine without changing the active pointer.

The first parallel Ubuntu aggregate exposed an inherited compiler/MSBuild server retaining redirected pipe handles in the
Milestone 6 sample verifier until its two-minute bound. The fixture now disables shared build servers for its nested commands;
the focused regression completed in 16 seconds and the full isolated Ubuntu integration suite passed 56/56.

## Security and portability

Definitions are data and cannot execute code. Only decode authority is valid. Integer endianness is explicit, hashes and
suite inputs are canonical, fuzz generation is fixed xorshift state, and operation counts replace timing-dependent promotion
evidence. Compiled plugins and real device I/O remain disabled.

## Rollback

Stop AgentForge and restore the complete pre-0022 database/artifact backup. Do not use down migration or edit active pointers.
The governed rollback transition restores the exact baseline only while the candidate hash is still active.
